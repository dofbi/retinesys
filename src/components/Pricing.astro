---
const plans = [
  {
    name: "Starter",
    price: "999",
    features: ["5 Locations", "7 Days", "Basic Analytics"]
  },
  {
    name: "Professional",
    price: "2499",
    features: ["15 Locations", "14 Days", "Advanced Analytics"]
  },
  {
    name: "Enterprise",
    price: "4999",
    features: ["Unlimited Locations", "30 Days", "Premium Support"]
  }
];
---
<section class="py-20 bg-gray-50">
  <div class="container mx-auto px-6">
    <h2 class="text-4xl font-bold text-center mb-16">Pricing Plans</h2>
    <div class="grid md:grid-cols-3 gap-8">
      {plans.map(plan => (
        <div class="bg-white p-8 rounded-lg shadow-lg text-center">
          <h3 class="text-2xl font-semibold mb-4">{plan.name}</h3>
          <p class="text-4xl font-bold mb-6">${plan.price}</p>
          <ul class="space-y-4 mb-8">
            {plan.features.map(feature => (
              <li>{feature}</li>
            ))}
          </ul>
          <a href="#upload" class="block bg-primary text-white px-6 py-3 rounded-full hover:bg-blue-600 transition-colors">
            Get Started
          </a>
        </div>
      ))}
    </div>
  </div>
</section>
